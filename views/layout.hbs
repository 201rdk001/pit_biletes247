<!DOCTYPE html>
<html lang="lv">

<head>
    <title>Biletes 24/7</title>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel='stylesheet' href='/stylesheet/style.css' />
    {{{block "styles"}}}
</head>

<body>
    <header>
        <h3>Biletes 24/7</h3>
        <div>
            <a href="/events">Pasākumi</a>
            <a href="/profile">Profils</a>
            {{#if is_admin}} <a href="/admin">Admin</a> {{/if}}
        </div>
        
        {{#if name}}
        <div><div>{{name}} | <a href="/logout">Iziet</a></div></div>
        {{else}}
        <div><div><a href="/login">Ieiet</a></div></div>
        {{/if}}
    </header>

    {{{body}}}
</body>

</html>